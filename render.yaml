services:
  - type: web
    name: karaoke-backend
    runtime: docker
    rootDir: karaoke/backend
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: CORS_ORIGINS
        value: "https://heaven2935-code.github.io"
